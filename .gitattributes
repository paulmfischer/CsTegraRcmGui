# Normalize to LF in the repo; Git checks out text files with the platform's
# native line ending unless overridden below.
* text=auto eol=lf

# Binary files — do not touch, do not diff as text.
*.dll binary
*.ico binary
*.png binary
